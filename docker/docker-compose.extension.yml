version: '3.8'

services:
  # Build de extensión
  extension-build:
    build:
      context: ../extension
      dockerfile: Dockerfile
    volumes:
      - ../extension/dist:/app/dist
    networks:
      - mirakl-network

  # Portal de pruebas
  portal:
    image: node:18-alpine
    container_name: mirakl-portal
    ports:
      - "3000:3000"
    working_dir: /app
    volumes:
      - ../tests:/app
    command: sh -c "npm install express cors && node portal-server.js"
    networks:
      - mirakl-network

networks:
  mirakl-network:
    driver: bridge
